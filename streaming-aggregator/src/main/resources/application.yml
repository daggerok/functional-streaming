spring:
  cloud:
    stream:
      bindings:
        error:
          destination: errors
        input:
          destination: messages
#          content-type: application/json
          durable-subscription: aggregator
        output:
          destination: aggregateMessages
#          content-type: application/json
  kafka:
    consumer:
      group-id: streaming-aggregator
      auto-offset-reset: earliest
  output:
    ansi:
      enabled: always
server:
  port: 0